'use strict';

describe('Directive: pointDiagram', function () {

  // load the directive's module
  beforeEach(module('algorithmsApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
